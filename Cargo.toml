[package]
name = "rs-utils"
version = "0.4.1"
authors = ["Shane Pearman <spearman@github.com>"]

[dependencies]
unwrap = "1.*"
lazy_static = "0.2.*"
num = "0.1.*"

[dev-dependencies]
quickcheck = "0.4.*"
quickcheck_macros = "0.4.*"
tempdir = "0.3.*"
macro-attr = "0.2.*"
enum_derive = "0.1.*"
